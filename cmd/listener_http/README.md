# HTTP/S Listener (Web Gateway)

This directory contains the specialized listener for handling **HTTP and HTTPS** traffic.

## Operational Role

This service binds to TCP ports 80 and 443. It serves as the primary high-bandwidth channel for command and control.
